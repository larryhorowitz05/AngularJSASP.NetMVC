/// <reference path="../../../scripts/ts/generated/viewModels.ts" />

// expand all the service view models to meet view requirements
module srv {

    export interface DeclarationsViewModel {
        DisableFields: boolean;
    };
}